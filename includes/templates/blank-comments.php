<?php
/**
 * Intentionally empty. Used in place of the active theme's own comments.php
 * (via the `comments_template` filter in Bpafb_Template_Frontend_Render)
 * when a matched Blockive Template has its "Hide comments" setting on.
 *
 * @package Blockive
 */

if (!defined('ABSPATH')) {
	exit; // Exit if accessed directly.
}
